{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "_note": "All scheduled jobs are paused — Nivi is fully stopped. Unipile (LinkedIn + WhatsApp) is disconnected. To revive, restore the entries in the `crons` array below from git history and redeploy after reconnecting Unipile.",
  "crons": []
}
